<%= form_with model:post, url: url do |f| %>
	<%= render 'layouts/error_messages', resource: news_letter %>
	<div class="flex">
		<div>
			<%= f.label :image, "画像" %>
			<div class="image_field">
				<%= image_tag (item.image.attached? ? item.image : 'no-image.png'), id: "img_prev", class: "admin_news_letter_image" %>
				<span class="btn btn-file btn-info"><%= f.file_field :image,  accept:'image/*' %>画像選択</span>
				<p>選択するとプレビューされます。<br>画像未選択のままだと、<br>「画像準備中」になります。</p>
			</div>
		</div>
		<table class="admin_post_info item_form">
			<thead>
				<tr>
					<th></th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td><%= f.label :title, "タイトル" %></td>
					<td><%= f.text_field :title %></td>
				</tr>
				<tr>
					<td><%= f.label :text, "本文" %></td>
					<td><%= f.text_area :text, rows: 3 %></td>
				</tr>
				<tr>
					<td><%= f.label :genre_id, "ジャンル" %></td>
					<td class="select_field"><%= f.collection_select :genre_id, Genre.all, :id, :name %></td>
				</tr>
				<tr>
				    <td>公開ステータス</td>
				</tr>
			</tbody>
		</table>
		<div class="flex align-center pl5">
			<%= f.submit submit, class: "btn btn-primary" %>
		</div>
	</div>
<% end %>